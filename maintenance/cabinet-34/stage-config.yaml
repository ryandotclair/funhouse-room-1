apiVersion: v1
kind: ConfigMap
metadata:
  name: funhouse-room-room1-stage
data:
  # Operator flips this for Stage A ↔ B (GitOps mutate).
  STAGE: "a"
  ROOM_ID: "room1"
  ROOM_TOKEN: "dycLMdmebafz"
  ROOMS_HOST: "riddler.rycr.io"
