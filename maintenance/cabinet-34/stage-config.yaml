apiVersion: v1
kind: ConfigMap
metadata:
  name: funhouse-room-room1-stage
  annotations:
    # Goon3: leave STAGE alone unless you mean it.
    # Goon17: red console = revert last prod push (CD), not a STAGE toy.
data:
  # Desk page desired state. Do not confuse with the red-button prod rollback.
  STAGE: "a"
  ROOM_ID: "room1"
  ROOM_TOKEN: "GJ2vQ7bXoCoQ"
  ROOMS_HOST: "riddler.rycr.io"
