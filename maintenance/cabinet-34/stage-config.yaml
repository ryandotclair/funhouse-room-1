apiVersion: v1
kind: ConfigMap
metadata:
  name: funhouse-room-room1-stage
  annotations:
    # Goon3: leave STAGE alone unless you mean it.
    # Goon17: the red button reverts this via Continuous Deployment. Not a toy.
data:
  # a = quiet desk page; b = loud chase marquee (do not "test in prod")
  STAGE: "a"
  ROOM_ID: "room1"
  ROOM_TOKEN: "GJ2vQ7bXoCoQ"
  ROOMS_HOST: "riddler.rycr.io"
